{
  "name": "foodalix-backend",
  "version": "1.0.0",
  "description": "A node.js web server that fetches data from a third-party (Swiggy) API and exposes it to a client",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jagannath Nayak",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "cross-fetch": "^4.0.0",
    "express": "^4.18.2"
  }
}
